volumes:
    redis_data: {}

services:
  redis:
    image: redis:latest
    container_name: gb_redis
    ports:
      - "6379:6379" 
    volumes:
      - redis_data:/data  
    networks:
      - hw03


networks:
  hw03:
    ipam:
      driver: default
      config:
        - subnet: 172.32.0.0/16